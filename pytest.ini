[pytest]
# Auto-detect and run async test functions without per-test @pytest.mark.asyncio.
# Without this, every `async def test_*` fails with "async def not natively supported".
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
